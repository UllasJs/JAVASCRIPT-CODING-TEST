const string = "is";

function indexoff() {
  const indexes = string.substring();
  for (let i = 0; i < indexes.length; i++) {
    console.log("index of " + indexes[i] + " : " + i);
  }
}

indexoff(string);